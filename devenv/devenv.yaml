# devenv configuration file
# Note: using same release as for the nic configuration
# See https://devenv.sh/reference/yaml-options/ for more options
allowUnfree: true
inputs:
  nixpkgs:
    url: github:NixOS/nixpkgs/nixos-23.11
  nixpkgs-unstable:
    url: github:NixOS/nixpkgs/nixos-unstable
